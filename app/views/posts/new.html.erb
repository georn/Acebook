<h1> Add a Post </h1>
<%= form_for @post do |form| %>
  <%= form.label :message %>
  <%= form.text_area :message, cols:80, rows:4, maxlength: 360  %>
  <br> <br>
  <%= form.submit "Submit" %>

<% end %>
